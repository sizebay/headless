{
  "tasks": {
    "dev": "deno run --watch src/sizebay.ts",
    "format": "deno run -A npm:@biomejs/biome format --write ./src",
    "deploy": "bash deploy.sh"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
